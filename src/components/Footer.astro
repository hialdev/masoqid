---
import WhatsAppButton from "./WhatsAppButton.astro";
import LanguageSwitcher from "./LanguageSwitcher";
import footerData from "../data/footer.json";
---

<footer
   class="bg-background-light border-t border-gray-200 pt-16 pb-8"
   id="contact"
>
   <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div
         class="flex flex-col md:flex-row justify-between items-start gap-12 mb-12"
      >
         <!-- Brand Section -->
         <div class="max-w-md">
            <div class="flex items-center gap-2 mb-4">
               <img src="/logo.png" alt="MASOQ.id" class="h-6" />
            </div>
            <p
               class="text-text-muted text-sm leading-relaxed mb-6"
               data-i18n="footer.desc"
            >
               Membuat absensi jujur, mudah, dan cerdas. Kami membantu
               perusahaan mengurangi penipuan dan meningkatkan produktivitas
               dengan validasi cerdas.
            </p>
            <div class="flex gap-4">
               {
                  footerData.social.length > 0 && footerData.social.map((social: any) => (
                     <a
                        class="text-gray-400 hover:text-primary transition-colors"
                        href={social?.url}
                        aria-label={social?.name}
                        set:html={social?.icon}
                     />
                  ))
               }
            </div>
         </div>

         <!-- Contact Section -->
         <div class="flex flex-col gap-4">
            <h3
               class="text-sm font-bold uppercase tracking-wider text-text-main"
               data-i18n="footer.contact"
            >
               Hubungi Kami
            </h3>
            <div class="flex flex-col gap-3">
               <a
                  class="flex items-center gap-2 text-text-muted hover:text-primary transition-colors"
                  href={`tel:${footerData.contact.phone}`}
               >
                  <span class="material-symbols-outlined text-lg">call</span>
                  <span>{footerData.contact.phone}</span>
               </a>
               <a
                  class="flex items-center gap-2 text-text-muted hover:text-primary transition-colors"
                  href={`mailto:${footerData.contact.email}`}
               >
                  <span class="material-symbols-outlined text-lg">mail</span>
                  <span>{footerData.contact.email}</span>
               </a>
               <div class="flex items-center gap-2 text-text-muted">
                  <span class="material-symbols-outlined text-lg"
                     >location_on</span
                  >
                  <span>{footerData.contact.location}</span>
               </div>
            </div>
         </div>

         <!-- CTA Section -->
         <div class="flex flex-col gap-4 min-w-[200px]">
            <h3
               class="text-sm font-bold uppercase tracking-wider text-text-main"
               data-i18n="footer.discuss"
            >
               Mari Diskusi
            </h3>
            <p
               class="text-sm text-text-muted mb-2"
               data-i18n="footer.discuss_desc"
            >
               Siap mengamankan sistem absensi Anda?
            </p>
            <WhatsAppButton
               text="Hubungi Kami"
               variant="full"
               className="shadow-none py-2"
               i18nKey="footer.contact"
            />
         </div>
      </div>

      <!-- Bottom Bar -->
      <div
         class="border-t border-gray-200 pt-8 flex flex-col md:flex-row justify-between items-center gap-4"
      >
         <div class="flex flex-col md:flex-row items-center gap-4 md:gap-8">
            <p class="text-sm text-gray-400">
               {footerData.credits}
            </p>
            <LanguageSwitcher client:load direction="up" />
            <!-- <div class="relative group">
               <button
                  class="flex items-center gap-2 text-sm font-medium text-gray-500 hover:text-primary transition-colors py-1"
               >
                  <img
                     alt="Indonesia"
                     class="w-4 h-4 rounded-full object-cover"
                     src="https://flagcdn.com/w40/id.png"
                  />
                  <span>Indonesia</span>
                  <span class="material-symbols-outlined text-base"
                     >expand_less</span
                  >
               </button>
               <div
                  class="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 w-32 hidden group-hover:block transition-all duration-200 opacity-0 group-hover:opacity-100 z-20"
               >
                  <div
                     class="bg-white rounded-lg shadow-lg border border-gray-100 overflow-hidden py-1"
                  >
                     <a
                        class="flex items-center gap-3 px-3 py-2 text-xs text-gray-700 hover:bg-gray-50 transition-colors"
                        href="#"
                     >
                        <img
                           alt="English"
                           class="w-4 h-4 rounded-full object-cover"
                           src="https://flagcdn.com/w40/gb.png"
                        />
                        English
                     </a>
                     <a
                        class="flex items-center gap-3 px-3 py-2 text-xs text-gray-700 hover:bg-gray-50 transition-colors"
                        href="#"
                     >
                        <img
                           alt="Chinese"
                           class="w-4 h-4 rounded-full object-cover"
                           src="https://flagcdn.com/w40/cn.png"
                        />
                        Chinese
                     </a>
                  </div>
               </div>
            </div> -->
         </div>

         <div class="flex gap-6">
            <a
               class="text-sm text-gray-400 hover:text-primary transition-colors"
               href="#"
               data-i18n="footer.privacy">Kebijakan Privasi</a
            >
            <a
               class="text-sm text-gray-400 hover:text-primary transition-colors"
               href="#"
               data-i18n="footer.terms">Syarat & Ketentuan</a
            >
         </div>
      </div>
   </div>
</footer>
-->

